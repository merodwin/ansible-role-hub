[Unit]
Description=JetBrains Hub
Requires=network.target
After=syslog.target network.target

[Service]
Type=forking
ExecStart={{hub_install_folder}}/latest/bin/hub.sh start
ExecStop={{hub_install_folder}}/latest/bin/hub.sh stop
User={{hub_user}}

[Install]
WantedBy=multi-user.target